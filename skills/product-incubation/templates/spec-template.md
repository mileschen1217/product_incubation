---
area: {PREFIX}      # VIS, USR, KPI, FUNC, DATA, API, UX, ARCH, SEC, PERF, I18N, DEC, TEST, OBS, DEPLOY, OPS, ROAD
title: {Area Title}
status: draft       # draft | review | approved
date: YYYY-MM-DD
---

# {Area Title}

> **Status markers:** Append `✓` (done) or `◐` (in progress) to requirement headings. No marker = not started. The `/product-incubation:status` command reads `###` headings.

<!--
  Adapt the sections below to fit this area. Use the "Done when" criteria
  from SKILL.md as your section headings. Examples:

  FUNC  → Input / Process / Output / Scope Boundaries
  VIS   → Problem Statement / Target User / Market Context
  USR   → User Journey / Acceptance Criteria
  KPI   → Metric / Target / Measurement Method
  DATA  → Entities / Relationships / Constraints / Schema
  API   → Endpoints / Request-Response Shapes / Error Codes
  UX    → Design Direction / Breakpoints / a11y / Interaction States
  SEC   → Auth Model / Input Validation / Threat Model
  PERF  → Load Targets / Latency Budgets / Scaling Strategy
  TEST  → Coverage Plan / CI Pipeline / Test Types
-->

## Requirements

### {PREFIX}-001: [First requirement]

Clear description of what must be true when this is done.

### Acceptance Criteria

- [ ] Criterion 1
- [ ] Criterion 2
- [ ] Criterion 3

### Dependencies

- Depends on: {PREFIX}-{NNN} (if any)
- Blocks: {PREFIX}-{NNN} (if any)

## Notes

Any additional context, open questions, or design considerations.
